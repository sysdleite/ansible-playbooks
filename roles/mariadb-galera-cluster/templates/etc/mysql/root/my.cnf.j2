# {{ ansible_managed }}

[client]
user=root
password="{{ root_password }}"

[mysql]
user=root
password="{{ root_password }}"

[mysqldump]
user=root
password="{{ root_password }}"

[mysqldiff]
user=root
password="{{ root_password }}"
